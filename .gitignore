# Generated by Cargo
# will have compiled files and executables
/target
/docker
.DS_Store
.env
# These are backup files generated by rustfmt
**/*.rs.bk

# Claude Code local config/session state — not relevant to the app
.claude/
CLAUDE.local.md

# Deploy tooling, local deploy config, and one-off SQL migrations run
# directly against the hosted Supabase project — all internal ops/infra
# detail, not part of the app itself, and too much detail about the live
# schema/security posture and deploy setup to be comfortable public.
/scripts/

# Internal working notes — not meant for a public repo
DESIGN_PROGRESS.md
AGENTS.md

# Marketing site (blackserverbook.com) — separate static site, not part of the app
/marketing/

