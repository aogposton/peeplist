# Copy this file to .env.deploy (same directory) and fill in your server details.
# .env.deploy is gitignored — it's local config, not meant to be committed.

DEPLOY_HOST=your.server.hostname.or.ip
DEPLOY_PATH=/var/www/peeplist

# Optional — defaults shown
# DEPLOY_USER=your-ssh-user
# DEPLOY_PORT=22
